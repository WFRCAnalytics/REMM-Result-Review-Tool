[project]
name = "remm-result-review"
version = "0.1.0"
description = "Local notebook and static review site for REMM result review maps."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "geopandas>=1.0",
    "ipykernel>=6.29",
    "jupyterlab>=4.2",
    "pandas>=2.2",
    "pyarrow>=15.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
